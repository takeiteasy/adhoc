"""ad — a math-notation calculator language.

Python implementation. The Rust implementation this replaces lives in git
history (`rust-final` tag at cutover); see docs/rewrite-plan.md for the
staged plan and docs/architecture.md for the pipeline.
"""

__version__ = "0.0.1"
